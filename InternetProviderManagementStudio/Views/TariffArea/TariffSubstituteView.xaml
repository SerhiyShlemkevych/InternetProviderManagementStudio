<Page x:Class="InternetProviderManagementStudio.Views.TariffArea.TariffSubstituteView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:InternetProviderManagementStudio.Views.TariffArea"
      mc:Ignorable="d" 
      d:DesignHeight="150" d:DesignWidth="600"
      Title="TariffSubstituteView">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="80"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Label Content="Setect another tariff plan to substitute" Grid.Row="0" Grid.Column="0"/>

        <ComboBox Grid.Row="1" Margin="5,5" ItemsSource="{Binding SubstituteItems}" SelectedItem="{Binding SubstituteItem}" Grid.Column="0">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <Label Content="{Binding Path=Name}"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        

    </Grid>
</Page>
